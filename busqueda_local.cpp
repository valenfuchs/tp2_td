#include <string>
#include <iostream>
#include <list>
#include <vector>
#include <fstream>
#include <sstream>
#include <chrono>

// pair<int, float> swapGAP(vector<vector<int>> distancias, vector<vector<int>> demandas, vector<int> capacidades) {
//     vector<vector<int>> solucion = depositoMasCercano(distancias, demandas, capacidades).second;

//     for(int i = 0; i < solucion.size(); ++i) {
//         for(int j = 0; j < solucion[i].size(); ++j) {

//         }
//     }
// }